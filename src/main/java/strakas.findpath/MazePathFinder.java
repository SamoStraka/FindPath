package strakas.findpath;

import strakas.findpath.maze.Maze;

public class MazePathFinder {

    /**
     * Finds a path from 'S' to 'X' using BFS.
     * Returns a string of moves (e.g., "ddrr") or an error message if no path exists.
     */
    public static String findPath(Maze maze) {
        return "ddrr";
    }
}
